<Page x:Class="ConsultPlanner.Views.UserList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ConsultPlanner.Views"
      mc:Ignorable="d" 
      Background="#242424"
      FontFamily="Segoe UI Light"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UserList">
    <Page.Resources>
        <Style x:Key="TableCell" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
        
        <Style x:Key="TableElementBlack" TargetType="TextBlock" BasedOn="{StaticResource TableCell}">
            <Setter Property="Width" Value="84"/>
            <Setter Property="Background" Value="#303030"/>
        </Style>
        <Style x:Key="TableElementWhite" TargetType="TextBlock" BasedOn="{StaticResource TableCell}">
            <Setter Property="Width" Value="85"/>
            <Setter Property="Background" Value="#4c4c4c"/>
        </Style>
        <Style x:Key="StackElementWhite" TargetType="StackPanel">
            <Setter Property="Background" Value="#4c4c4c"/>
        </Style>
        <Style x:Key="TableButton" TargetType="Button">
            <Setter Property="Background" Value="#303030"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="0.6"/>
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="15"/>
            <Setter Property="Margin" Value="2 0"/>
        </Style>
        <Style x:Key="Button1" TargetType="Button">
            <Setter Property="Background" Value="#242424"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="0.8"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="5 5"/>
            <Setter Property="Padding" Value="10"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#474747"/>
                    <Setter Property="TextBlock.TextDecorations" Value="Underline"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="1" Style="{StaticResource ResourceKey=Button1}" Content="Добавить" Command="{Binding DataContext.AddUserCommand, RelativeSource={RelativeSource AncestorType=Page}}"/>
        <ListView ItemsSource="{Binding Users, UpdateSourceTrigger=PropertyChanged}"
                  d:ItemsSource="{d:SampleData ItemCount=3}"
                  Background="#242424" BorderThickness="0"
                  Grid.Row="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Hidden">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding LastName}" Style="{StaticResource ResourceKey=TableElementWhite}"/>
                        <TextBlock Text="{Binding FirstName}" Style="{StaticResource ResourceKey=TableElementBlack}"/>
                        <TextBlock Text="{Binding Patronymic}" Style="{StaticResource ResourceKey=TableElementWhite}"/>
                        <TextBlock Text="{Binding Birthday}" Style="{StaticResource ResourceKey=TableElementBlack}"/>
                        <TextBlock Text="{Binding Phone}" Style="{StaticResource ResourceKey=TableElementWhite}"/>
                        <TextBlock Text="{Binding Email}" Style="{StaticResource ResourceKey=TableElementBlack}"/>
                        <TextBlock Text="{Binding RegisterDate}" Style="{StaticResource ResourceKey=TableElementWhite}"/>
                        <TextBlock Text="{Binding Password}" Style="{StaticResource ResourceKey=TableElementBlack}"/>
                        <Border Padding="2" Background="#4c4c4c">
                            <StackPanel Orientation="Horizontal" Style="{StaticResource ResourceKey=StackElementWhite}">
                                <Button Style="{StaticResource ResourceKey=TableButton}" Command="{Binding DataContext.EditUserCommand, RelativeSource={RelativeSource AncestorType=Page}}" CommandParameter="{Binding}">
                                    <Image Source="../Resources/Images/Edit.png"/>
                                </Button>
                                <Button Style="{StaticResource ResourceKey=TableButton}" Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=Page}}" CommandParameter="{Binding}">
                                    <Image Source="../Resources/Images/Exit.png"/>
                                </Button>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
